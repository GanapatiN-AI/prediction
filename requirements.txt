# Web framework
fastapi
uvicorn[standard]

# Data processing
pandas
python-dateutil
openpyxl

# Google ADK and GenAI (core chatbot dependencies)
google-adk
google-genai

# For typing and linting (optional, but recommended for modern Python)
typing-extensions

# For async support (already included in FastAPI, but explicit for clarity)
httpx

# For running in production (optional, for deployment)
